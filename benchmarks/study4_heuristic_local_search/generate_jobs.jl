"""
`generate_jobs.jl` -- placeholder. Study 4 (heuristic pricing frontier, provisional
method name `local_search` -- see `README.md`) has no design yet, so there's no design
grid to expand into `config/jobs.tsv`. Once the heuristic's actual knobs are chosen
(candidate-set restriction, reward thresholds, beam width, warm starts, exact-pricing
frequency -- see `README.md`'s "Design choices to evaluate"), this should follow
`../study1_formulation_lp_ip_gap/generate_jobs.jl`'s shape: expand the chosen knob grid
crossed with instance size into a tab-separated `config/jobs.tsv`.

TODO: not implemented -- blocked on the heuristic's design existing at all, not just its
implementation.
"""

# TODO: implement, once there is a design to sweep.
