# Other

Everything no other page claimed. This page exists so that adding a subsystem cannot
silently drop it from the documentation: `make.jl` routes unclaimed source files here and
logs what fell through, rather than leaving them unrendered.

If something below belongs on a real page, give that page's `{{autodocs}}` marker the
directory — the file list is derived from the source tree, so no list needs editing.

{{autodocs-rest}}
